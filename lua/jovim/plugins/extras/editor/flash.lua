require("lazy.core.util").warn(
  "`flash.nvim` is now the default jump plugin for **JoVim**.\nPlease remove the `jovim.plugins.extras.editor.flash` import from your **lazy** config.",
  { title = "JoVim" }
)

return {}
